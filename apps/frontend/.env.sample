# Resume Matcher Frontend Configuration
# Copy this file to .env.local and update values as needed

# ===========================================
# API Configuration
# ===========================================
# Backend API URL - defaults to "/" (same-origin, proxied by Next.js rewrites).
# Only change this if running the backend on a separate host/port during development.
# NEXT_PUBLIC_API_URL=/

# ===========================================
# Code Quest Resume Lab embed mode (optional)
# ===========================================
# Comma-separated origins allowed to iframe this app and send postMessage parent identity.
# Also used for CSP frame-ancestors. Defaults: http://localhost:5000,http://127.0.0.1:5000
# NEXT_PUBLIC_CODEQUEST_EMBED_PARENT_ORIGINS=http://localhost:5000,http://127.0.0.1:5000
#
# Activate embed UI: ?embed=codequest&parentOrigin=http://localhost:5000
# (or #embed=codequest). Standalone mode is unchanged when the flag is absent.
# See docs/CODEQUEST_EMBED.md

# ===========================================
# Development Server (optional)
# ===========================================
# To change the frontend port, use: npm run dev -- -p 3001
# Or add to package.json scripts: "dev": "next dev --turbopack -p 3001"
#
# If you change the frontend port, make sure to update:
# 1. Backend CORS_ORIGINS to include your new frontend URL
# 2. Backend FRONTEND_BASE_URL to point to your new frontend URL